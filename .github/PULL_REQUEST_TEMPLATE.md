**What does this change, seen from the picker or from the config?**

- [ ] `just test` and `just lint` pass
- [ ] Deciding logic arrived as a pure function with its test
- [ ] README updated in the same PR if a config key or a behavior changed
- [ ] No new dependency, or the case for it is made above (what it buys, what
      it costs in `go.sum`, why the standard library cannot)
